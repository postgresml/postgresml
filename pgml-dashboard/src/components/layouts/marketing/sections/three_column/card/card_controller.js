import { Controller } from '@hotwired/stimulus'

export default class extends Controller {
  static targets = [];
  static outlets = [];

  initialize() {
    console.log("Initialized layouts-marketing-section-three-column-card");
  }

  connect() {}

  disconnect() {}
}